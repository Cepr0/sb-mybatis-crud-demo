logging:
  level:
#    root: debug
    jdbc:
      sqltiming: info
      resultsettable: info
      sqlonly: fatal
      audit: fatal
      resultset: fatal
      connection: fatal
    org:
#      springframework:
#        web: debug
#        data: debug
#        transaction: debug
      mybatis.spring: debug
    io.github.cepr0: debug

log4jdbc:
  dump.sql:
    addsemicolon: true
    maxlinelength: 0
  trim.sql:
    extrablanklines: false

